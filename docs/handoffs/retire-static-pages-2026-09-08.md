# Retire Static Pages — Log-Only Batch (2026-09-08)

## Scope

Log-only retirement candidate list from the static chrome-rule audit. No files
are deleted in this pass; they are recorded here with reachability and
proposed disposition so a future pass can dispose of them without re-auditing.

## Candidates

| File | Reachability | Proposed disposition |
|---|---|---|
| `static/components/footer.html` | Non-canonical partial; only referenced by `module_converter.html` (deleted in PR #177) | **Retire** — superseded by `app/templates/components/footer.html` (PR #180) |
| `static/components/unified-footer.html` | Non-canonical partial; same as above | **Retire** — superseded by PR #180 |
| `static/ai-helper.html` | Live route `/ai-helper` exists (`app/main.py:2777`) but **zero inbound nav links** from the app | **Decision needed** — either link it from help/tools or retire if unused |
| `static/filedored.html` | No route, no TemplateResponse reference, no inbound links | **Retire** after one release-cycle confirmation |
| `static/library.html` (root) | Superseded by Jinja `pages/library.html` (`/law-library`) | **Retire** |
| `static/search.html` | No route; only outbound link is to dead `/static/library.html` | **Retire** |
| `static/status.html` | No route, no refs | **Retire** |
| `static/reconnect/index.html` | Orphan — `/storage/reconnect` is generated by `app/modules/onboarding/reconnect.py::_reconnect_html`; this static file is never served as a route | **Retire** |

## Notes

- `static/ai-helper.html` has a live route but is not reachable through normal
  navigation. A separate product decision is needed: expose it or remove the
  route and the file together.
- Onboarding-generated pages and `reconnect.py` are NO-TOUCH per
  `AGENTS.md`; this log intentionally does not touch them.
- Components partials are already removed in the `consolidate-footer-sources-2026-09-08`
  branch (PR #180). Once that PR merges, this batch can treat them as retired.

## Verification

- `python tools/verify_static_refs.py --scope static/` confirms the files above
  have no live route or `TemplateResponse` reference (where applicable).
- Inbound-link scan across `app/` and `static/` confirms `ai-helper.html` has
  zero internal links.
