# HANDOFF: P5 Close-Out + Remaining Open Items

**Status:** P5 algorithmic wiring is done. Main is clean. This doc covers what's still open, what to do with it, and how to close out the phase.

---

## 1. Where things stand

- PRs #79 (preserve-main batch, 19 tasks), #80 (vault ADR-0008 wiring), #81 (litigation_intelligence DSN security fix) — all merged, in that order, `sync_orchestrator.py --check` clean after each.
- Tracker on `main`: **90 total — 80 resolved, 9 pending, 1 review.**
- `main` working tree clean.

---

## 2. Enumerate the 9 pending tasks before deciding anything else

These weren't part of the P5 batch — get the actual list before assuming what's in it:

```
python -c "
import json
data = json.load(open('tools/agent_orchestrator_tasks.json'))
for t in data:
    if t['status'] == 'pending':
        print(t['id'], '-', t.get('title', t.get('description', '')[:80]))
"
```

Report this list back before starting any of them. Don't assume they're all P4/P5 leftovers — some may predate this whole reconciliation effort.

---

## 3. Three items NOT to close automatically — each needs a specific human/agent action, not a batch resolution

### a. `phase2-dc4e66-065` — progress_tracker/dashboard wiring review
Caller migration done (PR #77). The wiring review half is still open. Needs someone to actually look at `app/modules/dashboard/service.py` and `app/modules/progress/router.py` wiring, not just confirm imports resolve. Assign as its own task — don't fold into whatever picks up the 9 pending items.

### b. `phase2-50551b-067` — `app/modules/security/router.py`
Sitting on `devin/p5-review-flags`, unmerged, by design. Needs a one-line equivalence confirmation between `secure=not is_localhost` and `secure=False if is_localhost else True` — a test or explicit truth-table check — before it's resolved. Small task, but must not get rubber-stamped through a batch PR. Once confirmed, resolve individually.

### c. `phase2-1a1341-055` — `app/services/eviction/case_builder`
Also on `devin/p5-review-flags`, unmerged. Needs manual legal review (tenant-extraction refactor + added DB query in legal-output code) — this is a Tier A / human call, not something an agent resolves on its own judgment. Flag for Brad specifically, not for agent dispatch.

**Do not merge `devin/p5-review-flags` as-is.** It's a parking branch for these two items, not a resolution. Only turn it into a PR once (b) and (c) are independently confirmed/reviewed.

---

## 4. Other open threads from earlier in this reconciliation effort (don't lose track)

- **`todo-081`** — recurring Test-job-hang investigation (seen on PR #59 and PR #69, both tracker-only changes). Still open, still unexplained. Worth a dedicated look once the higher-priority reconciliation work clears, since it'll keep costing time on every future tracker-only PR.
- **`local/markdown-lint-pass`** — 278-file lint pass, still unpushed, still undecided (push+PR vs. merge vs. discard). Recommended earlier: push as PR once this reconciliation phase is fully clear, so its CI signal is easy to read against a settled main.

---

## 5. Suggested order to close this phase out

1. Enumerate the 9 pending tasks (section 2) and report back.
2. Resolve (b) `security/router.py` — smallest, fastest.
3. Route (c) `eviction/case_builder` to Brad for legal-review sign-off — not agent-actionable.
4. Assign `phase2-dc4e66-065` wiring review as a standalone task.
5. Once (b)–(d) have owners/status, work the 9 pending tasks.
6. Once tracker is fully resolved or every remaining item has a clear non-agent owner, that closes the ADR-0008 pilot reconciliation effort. At that point: revisit `todo-081` and `local/markdown-lint-pass` as the next things to pick up.

---

## 6. Standing rules — unchanged, still apply

- No `git reset --hard` without explicit human confirmation.
- One task per commit, no self-approval, preflight full-file reads.
- Commit messages: `admin:` / `user:` / `help:` / `adr:` prefix required (pre-commit hook).
- `sync_orchestrator.py --check` after every merge, no exceptions.
- Security/auth/permissions-touching diffs get individual review regardless of diff size — never batch them.
