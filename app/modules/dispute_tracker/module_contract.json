{
  "module_name": "Dispute Tracker",
  "layout": "app/modules/dispute_tracker/ — 4 registered function groups (dispute_tracker_module, dispute_tracker_list, dispute_tracker_create, dispute_tracker_compare). Derived record: page layout not yet described.",
  "inputs": [
    {
      "name": "comparison",
      "type": "field",
      "required": true
    },
    {
      "name": "dispute",
      "type": "field",
      "required": true
    },
    {
      "name": "dispute_id",
      "type": "field",
      "required": true
    },
    {
      "name": "user_id",
      "type": "field",
      "required": true
    }
  ],
  "input_audit": [
    {
      "input_name": "comparison",
      "source": "other",
      "note": "Derived from FunctionGroupContract; real input source not yet recorded."
    },
    {
      "input_name": "dispute",
      "source": "other",
      "note": "Derived from FunctionGroupContract; real input source not yet recorded."
    },
    {
      "input_name": "dispute_id",
      "source": "other",
      "note": "Derived from FunctionGroupContract; real input source not yet recorded."
    },
    {
      "input_name": "user_id",
      "source": "other",
      "note": "Derived from FunctionGroupContract; real input source not yet recorded."
    }
  ],
  "preview_state": null,
  "review_state": null,
  "output_type": "other",
  "output_how": "Registered function groups return named outputs (comparison, comparison_id, count, dispute, dispute_id, disputes, health) via the module's routes/services.",
  "output_where": "/api/dispute-tracker/, /api/dispute-tracker/comparisons, /api/dispute-tracker/disputes, /api/dispute-tracker/health (prefixes: /api/dispute-tracker)",
  "output_why": "Serves the Dispute Tracker functions declared in its FunctionGroupContracts.",
  "process_description": "CANONICAL module metadata. Returns health and capabilities. T2 module — tenant PII may appear in dispute/comparison records. CANONICAL list disputes for the authenticated user. Returns dispute metadata (no full PII content; content lives in cloud overlay per DB boundary rule). CANONICAL create a new dispute record. Stores pointers/structure only; PII content is written to the user's cloud overlay. CANONICAL create or update a fee/term comparison entry attached to a dispute. Stores metadata and comparison summary; supporting documents are overlays.",
  "process_context": "Derived record. Invoked via the dispute_tracker module's registered routes and dependencies.",
  "narrative_events": []
}
