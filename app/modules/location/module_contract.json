{
  "module_name": "Location",
  "layout": "app/modules/location/ — 10 registered function groups (location_current, location_update, location_clear, location_supported_states, location_state_info, location_legal_resources, location_eviction_timeline, location_mn_counties, location_county_info, location_context). Derived record: page layout not yet described.",
  "inputs": [
    {
      "name": "county",
      "type": "field",
      "required": false
    },
    {
      "name": "state_code",
      "type": "field",
      "required": false
    },
    {
      "name": "user_id",
      "type": "field",
      "required": true
    }
  ],
  "input_audit": [
    {
      "input_name": "county",
      "source": "other",
      "note": "Derived from FunctionGroupContract; real input source not yet recorded."
    },
    {
      "input_name": "state_code",
      "source": "other",
      "note": "Derived from FunctionGroupContract; real input source not yet recorded."
    },
    {
      "input_name": "user_id",
      "source": "other",
      "note": "Derived from FunctionGroupContract; real input source not yet recorded."
    }
  ],
  "preview_state": null,
  "review_state": null,
  "output_type": "other",
  "output_how": "Registered function groups return named outputs (counties, county, court_location, eviction_timeline, jurisdiction_metadata, local_resources, name, resources…) via the module's routes/services.",
  "output_where": "app.modules.location function outputs",
  "output_why": "Serves the Location functions declared in its FunctionGroupContracts.",
  "process_description": "CANONICAL get the user's current location. Returns state code, county (if known), and whether it was auto-detected or manually set. Defaults to Minnesota if no location is set. CANONICAL update the user's location. The tenant can manually set their state and county. This drives jurisdiction-aware content throughout the app. CANONICAL clear the user's saved location. Resets to Minnesota default. Used when the tenant moves or wants to reset. CANONICAL list of states supported by the location service. Returns state codes and names. Used by the state selector dropdown. CANONICAL state information for a specific state code. Returns state name, supported counties, and available resources. CANONICAL jurisdiction-aware legal resources for the user. Returns legal aid organizations, tenant rights groups, and government agencies for the user's state and county. Facts only, no recommendations. CANONICAL eviction procedure timeline for the user's jurisdiction. Returns the steps, notice periods, and deadlines for an eviction in the user's state. Facts only — not legal advice. CANONICAL list of Minnesota counties. MN is the default and most complete jurisdiction. Returns county names and codes. CANONICAL county-specific information. Returns county details, court locations, and local resources for the given county. CANONICAL full location context for the user. Returns state, county, legal resources, eviction timeline, and jurisdiction metadata in one call. Used by the tenant dashboard to populate jurisdiction-aware sections.",
  "process_context": "Derived record. Invoked via the location module's registered routes and dependencies.",
  "narrative_events": []
}
