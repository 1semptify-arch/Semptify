{
  "module_name": "Admin Auth",
  "layout": "app/modules/admin_auth/ — 1 registered function groups (admin_login). Derived record: page layout not yet described.",
  "inputs": [
    {
      "name": "password",
      "type": "field",
      "required": true
    },
    {
      "name": "totp_code",
      "type": "field",
      "required": false
    },
    {
      "name": "username",
      "type": "field",
      "required": true
    }
  ],
  "input_audit": [
    {
      "input_name": "password",
      "source": "other",
      "note": "Derived from FunctionGroupContract; real input source not yet recorded."
    },
    {
      "input_name": "totp_code",
      "source": "other",
      "note": "Derived from FunctionGroupContract; real input source not yet recorded."
    },
    {
      "input_name": "username",
      "source": "other",
      "note": "Derived from FunctionGroupContract; real input source not yet recorded."
    }
  ],
  "preview_state": null,
  "review_state": null,
  "output_type": "other",
  "output_how": "Registered function groups return named outputs (auth_response) via the module's routes/services.",
  "output_where": "/admin/api/login-step1, /admin/api/login-step2 (prefixes: /admin/api)",
  "output_why": "Serves the Admin Auth functions declared in its FunctionGroupContracts.",
  "process_description": "Public-facing admin authentication endpoints. POST /admin/api/login-step1 validates credentials. POST /admin/api/login-step2 validates TOTP and issues the admin elevation cookie. No prior admin session is required.",
  "process_context": "Derived record. Invoked via the admin_auth module's registered routes and dependencies.",
  "narrative_events": []
}
