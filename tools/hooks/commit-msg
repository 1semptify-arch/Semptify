#!/bin/sh
# Git commit-msg hook: require a category prefix when the commit touches
# something mapped in docs/doc-map.yaml.
# Set as active with: git config core.hooksPath tools/hooks
set -e

./venv311/Scripts/python.exe tools/hooks/check_commit_msg.py $1
